---
phase: 01-app-shell
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/routes/+page.svelte
  - src/lib/components/SurfaceRail.svelte
  - src/lib/components/WorkspaceShell.svelte
  - src/lib/components/SurfacePanel.svelte
  - src/lib/components/StatusRegion.svelte
  - src/lib/stores/surface.ts
  - tests/rust/app_shell.rs
autonomous: true
requirements:
  - SHELL-02
must_haves:
  truths:
    - The user can switch between chat, history, settings, and artifact surfaces from the shell.
    - Focus order and keyboard activation work for the surface switcher.
    - The active surface survives a restart through backend-owned persistence.
  artifacts:
    - src/lib/components/WorkspaceShell.svelte
    - src/lib/components/SurfacePanel.svelte
    - src/lib/components/StatusRegion.svelte
    - tests/rust/app_shell.rs
  key_links:
    - Surface navigation updates the shell view and the stored active surface together.
    - Keyboard and screen-reader users can perceive the active surface and status changes.
    - The smoke coverage proves launch, switch, and restore behavior end to end.
---

<objective>
Refine the Phase 1 shell into an accessible, keyboard-navigable workspace switcher and lock the shell behavior down with smoke coverage for launch, switch, and restore.

Purpose: make the workspace shell feel like a real desktop surface rather than static chrome.
Output: accessible navigation components, status feedback, and a backend smoke test for the shell state round trip.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/architecture.md
@docs/privacy-boundaries.md
@docs/threat-model.md
@docs/Tauri_Svelte_AI_App_Architecture_Adversarial_Hardened_v5.md
@src-tauri/AGENTS.md
@src-tauri/src/AGENTS.md
@src-tauri/src/ipc/AGENTS.md
@src-tauri/src/storage/AGENTS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/privacy-boundaries.md
@docs/threat-model.md
@docs/Tauri_Svelte_AI_App_Architecture_Adversarial_Hardened_v5.md
@src-tauri/AGENTS.md
@src-tauri/src/AGENTS.md
@src-tauri/src/ipc/AGENTS.md
@src-tauri/src/storage/AGENTS.md
</context>

<tasks>
<task type="auto">
  <name>Make surface navigation accessible and status-aware</name>
  <files>src/routes/+page.svelte, src/lib/components/SurfaceRail.svelte, src/lib/components/WorkspaceShell.svelte, src/lib/components/SurfacePanel.svelte, src/lib/components/StatusRegion.svelte, src/lib/stores/surface.ts</files>
  <read_first>docs/design-blueprint.md, docs/Tauri_Svelte_AI_App_Architecture_Adversarial_Hardened_v5.md, docs/privacy-boundaries.md</read_first>
  <action>Add keyboard activation, visible focus treatment, and programmatic active-surface/status announcements to the shell. Keep the focus order aligned to the visual order of the workspace, and make the active surface obvious to assistive technology and sighted users at the same time.</action>
  <verify>Use keyboard-only navigation to move across the four surfaces and confirm the active state and status text change together.</verify>
  <acceptance_criteria>
    - Each surface can be selected without a pointer.
    - Focus state is visible in the shell.
    - The active surface is announced to screen readers and visible in the UI.
  </acceptance_criteria>
  <done>The workspace shell is usable as a real desktop navigation surface.</done>
</task>

<task type="auto">
  <name>Add shell smoke coverage</name>
  <files>tests/rust/app_shell.rs</files>
  <read_first>.planning/codebase/TESTING.md, docs/privacy-boundaries.md, src-tauri/src/storage/AGENTS.md</read_first>
  <action>Add a narrow Rust test for the shell preference round trip and startup hydration path. The test should prove the persisted active-surface value is written, read back, and used as the startup default without falling back to frontend-local storage.</action>
  <verify>Run `cargo test --workspace --all-targets` and confirm the shell-state test passes.</verify>
  <acceptance_criteria>
    - The backend test covers the write/read/restore path for the active surface.
    - The test names the shell persistence behavior in a grep-visible way.
  </acceptance_criteria>
  <done>The phase has at least one executable backend verification for the shell behavior.</done>
</task>
</tasks>

<verification>
Before declaring the plan complete:
- [ ] Keyboard-only navigation changes the active surface and keeps focus visible.
- [ ] The shell status region announces state changes.
- [ ] `cargo test --workspace --all-targets` passes with the new shell test.
</verification>

<success_criteria>
- The user can reach chat, history, settings, and artifact surfaces from the shell.
- The shell remains accessible and status-aware under keyboard-only use.
- The active surface survives a restart through backend-owned persistence.
</success_criteria>
