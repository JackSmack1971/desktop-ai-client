---
phase: 04-privacy
plan: 06
type: execute
wave: 4
depends_on: ["04-04", "04-05"]
files_modified:
  - src-tauri/src/main.rs
  - src-tauri/capabilities/main.json
  - src-tauri/permissions/privacy.toml
  - src-tauri/permissions/files.toml
autonomous: true
requirements: [SEC-01, SEC-02]
user_setup: []

must_haves:
  truths:
    - "all 5 new Phase 4 commands are registered in tauri::generate_handler![]"
    - "the dialog plugin is initialized in the Tauri builder"
    - "each new command has a matching permission identifier in main.json backed by a TOML permission entry"
    - "the app compiles and the full test suite stays green after wiring"
  artifacts:
    - path: "src-tauri/src/main.rs"
      provides: "5 command registrations + dialog plugin init"
      contains: "privacy_set_provider_key"
    - path: "src-tauri/capabilities/main.json"
      provides: "5 allow-* permission identifiers"
      contains: "allow-privacy-set-provider-key"
    - path: "src-tauri/permissions/privacy.toml"
      provides: "3 privacy permission entries"
      contains: "allow-privacy-set-provider-key"
    - path: "src-tauri/permissions/files.toml"
      provides: "2 files permission entries"
      contains: "allow-files-open-dialog"
  key_links:
    - from: "src-tauri/src/main.rs"
      to: "ipc::privacy / ipc::files commands"
      via: "generate_handler! registration"
      pattern: "privacy_set_provider_key"
    - from: "src-tauri/capabilities/main.json"
      to: "permissions/*.toml"
      via: "allow-* identifiers"
      pattern: "allow-files-open-dialog"
---

<objective>
Wire the five new Phase 4 commands into the Tauri command surface: register them in `generate_handler![]`, initialize the dialog plugin, add the two permission TOML files, and add the five `allow-*` identifiers to the main-window capability. This satisfies the command-registration invariant (every command appears in both `generate_handler!` and a capability grant).

Purpose: Without this wiring the commands are unreachable from the frontend. Depends on Plan 04 (privacy commands) and Plan 05 (files commands) existing. Implements the registration invariant for D-03/D-10/D-06.
Output: updated main.rs, main.json, new privacy.toml and files.toml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-privacy/04-CONTEXT.md
@.planning/phases/04-privacy/04-RESEARCH.md
@src-tauri/src/main.rs
@src-tauri/capabilities/main.json
@src-tauri/permissions/chat.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create privacy.toml and files.toml permission files</name>
  <files>src-tauri/permissions/privacy.toml, src-tauri/permissions/files.toml</files>
  <read_first>
    - src-tauri/permissions/chat.toml (exact `[[permission]]` + `[permission.commands]` TOML shape to replicate)
    - .planning/phases/04-privacy/04-RESEARCH.md (Pattern 9 permission TOML bodies for privacy + files)
  </read_first>
  <action>
    Create `src-tauri/permissions/privacy.toml` with three `[[permission]]` blocks: `allow-privacy-set-provider-key` (allow `["privacy_set_provider_key"]`), `allow-privacy-get-credential-status` (allow `["privacy_get_credential_status"]`), `allow-privacy-clear-provider-key` (allow `["privacy_clear_provider_key"]`). Each needs an `identifier`, a `description` matching the RESEARCH Pattern 9 text (status command description must note it returns status only, never the key value), and a `[permission.commands] allow = [...]`. Create `src-tauri/permissions/files.toml` with two `[[permission]]` blocks: `allow-files-open-dialog` (allow `["files_open_dialog"]`) and `allow-files-read-token` (allow `["files_read_token"]`), each describing that no raw path crosses the boundary. Match the exact TOML structure of `chat.toml`.
  </action>
  <verify>
    <automated>test -f src-tauri/permissions/privacy.toml && test -f src-tauri/permissions/files.toml && grep -c 'identifier =' src-tauri/permissions/privacy.toml && grep -c 'identifier =' src-tauri/permissions/files.toml</automated>
  </verify>
  <acceptance_criteria>
    - `privacy.toml` contains 3 `identifier =` lines: `allow-privacy-set-provider-key`, `allow-privacy-get-credential-status`, `allow-privacy-clear-provider-key`
    - `files.toml` contains 2 `identifier =` lines: `allow-files-open-dialog`, `allow-files-read-token`
    - each `[[permission]]` has a paired `[permission.commands] allow = [...]` with the matching command name
  </acceptance_criteria>
  <done>Two permission TOML files exist with 3 + 2 entries matching the chat.toml shape.</done>
</task>

<task type="auto">
  <name>Task 2: Register commands + dialog plugin in main.rs and grant capabilities</name>
  <files>src-tauri/src/main.rs, src-tauri/capabilities/main.json</files>
  <read_first>
    - src-tauri/src/main.rs (existing `generate_handler![]` list, plugin init chain, setup hook)
    - src-tauri/capabilities/main.json (existing `permissions` array)
    - .planning/phases/04-privacy/04-RESEARCH.md (IPC command registration example, dialog plugin init `.plugin(tauri_plugin_dialog::init())`)
    - .planning/phases/04-privacy/04-04-PLAN.md and 04-05-PLAN.md (exact command fn paths: ipc::privacy::*, ipc::files::*)
  </read_first>
  <action>
    In `main.rs`: add `.plugin(tauri_plugin_dialog::init())` to the builder chain (alongside the existing `tauri_plugin_opener::init()`). Append the five commands to `tauri::generate_handler![...]`: `ipc::privacy::privacy_set_provider_key`, `ipc::privacy::privacy_get_credential_status`, `ipc::privacy::privacy_clear_provider_key`, `ipc::files::files_open_dialog`, `ipc::files::files_read_token`. Preserve the existing 8 registrations.
    In `capabilities/main.json`: append the five identifiers to the `permissions` array: `allow-privacy-set-provider-key`, `allow-privacy-get-credential-status`, `allow-privacy-clear-provider-key`, `allow-files-open-dialog`, `allow-files-read-token`. Also add the dialog plugin's required default permission if the build requires it (e.g. `dialog:default`) — verify against the build; if `cargo build` reports a missing dialog capability, add `dialog:default`. Keep the existing identifiers intact.
  </action>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | grep -v '^#' | grep -ci 'error\[' ; cargo test --manifest-path src-tauri/Cargo.toml --all-targets 2>&1 | grep -v '^#' | grep -c 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `main.rs` `generate_handler!` contains all 5 new command paths plus the original 8
    - `main.rs` builder chain contains `tauri_plugin_dialog::init()`
    - `capabilities/main.json` `permissions` array contains all 5 new `allow-*` identifiers
    - `cargo build --manifest-path src-tauri/Cargo.toml` produces 0 `error[` lines
    - `cargo test --manifest-path src-tauri/Cargo.toml --all-targets` exits 0 (full suite green)
  </acceptance_criteria>
  <done>All 5 commands registered + dialog plugin initialized + capabilities granted; app builds; full suite green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 06)

- `src-tauri/permissions/privacy.toml` (3 permission entries)
- `src-tauri/permissions/files.toml` (2 permission entries)
- 5 new `generate_handler!` registrations in main.rs
- `tauri_plugin_dialog::init()` plugin registration
- 5 new `allow-*` identifiers in capabilities/main.json
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capability manifest → command surface | Only allow-listed commands are reachable from the main window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Elevation of Privilege | unallowlisted command surface | mitigate | every new command has a TOML permission + capability grant + generate_handler! entry (deny-by-inventory) |
| T-04-18 | Spoofing | dialog plugin overbroad grant | mitigate | only `dialog:default`/required permission added; picker invoked solely via backend command bound to main window |
</threat_model>

<verification>
- `cargo build --manifest-path src-tauri/Cargo.toml` produces no `error[` lines
- `cargo test --manifest-path src-tauri/Cargo.toml --all-targets` full suite green
- 5 new commands present in generate_handler! and main.json; 2 TOML files present
</verification>

<success_criteria>
- 5 commands registered; dialog plugin initialized
- privacy.toml + files.toml created; main.json grants all 5 identifiers
- App builds and full Rust suite green
</success_criteria>

<output>
Create `.planning/phases/04-privacy/04-06-SUMMARY.md` when done
</output>
