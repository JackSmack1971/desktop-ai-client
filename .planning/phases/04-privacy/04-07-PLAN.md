---
phase: 04-privacy
plan: 07
type: execute
wave: 5
depends_on: ["04-06"]
files_modified:
  - src/lib/stores/settings.ts
  - src/lib/components/surfaces/SettingsSurface.svelte
autonomous: false
requirements: [SEC-01]
user_setup: []

must_haves:
  truths:
    - "privacyStore exposes status/loading/error getters and loadStatus/setProviderKey/clearProviderKey using invoke + normalizeIpcError"
    - "the API key input is type=password, autocomplete=off, and its value is cleared from $state immediately after each IPC call (success and failure)"
    - "the UI never renders the key value back; status is only CONFIGURED/MISSING/loading"
    - "Remove key uses an inline alertdialog confirmation; the error region renders only when error is non-null with role=alert"
  artifacts:
    - path: "src/lib/stores/settings.ts"
      provides: "privacyStore with three IPC wrappers + CredentialStatus/ProviderId types"
      contains: "privacyStore"
    - path: "src/lib/components/surfaces/SettingsSurface.svelte"
      provides: "credential management UI per UI-SPEC"
      contains: "type=\"password\""
  key_links:
    - from: "src/lib/stores/settings.ts"
      to: "privacy_set_provider_key / privacy_get_credential_status / privacy_clear_provider_key"
      via: "invoke wrappers"
      pattern: "invoke<"
    - from: "src/lib/components/surfaces/SettingsSurface.svelte"
      to: "privacyStore"
      via: "store import + method calls"
      pattern: "privacyStore"
---

<objective>
Implement the single Phase 4 frontend deliverable: `privacyStore` (settings.ts) wrapping the three privacy IPC commands, and `SettingsSurface.svelte` replacing the scaffold with the credential-management UI exactly per UI-SPEC.md (status display, write-only password entry cleared after submit, inline remove confirmation, role=alert error region).

Purpose: Gives the user a way to set/clear/check their OpenRouter credential without the key ever returning to the renderer. Implements D-11, D-02, D-10. Depends on Plan 06 (commands registered + capabilities granted).
Output: settings.ts store, SettingsSurface.svelte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-privacy/04-CONTEXT.md
@.planning/phases/04-privacy/04-RESEARCH.md
@.planning/phases/04-privacy/04-UI-SPEC.md
@src/lib/stores/history.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement privacyStore in src/lib/stores/settings.ts</name>
  <files>src/lib/stores/settings.ts</files>
  <behavior>
    - Test: `normalizeIpcError({ code: 'CREDENTIAL_STORE_ERROR', message: 'x' })` returns `'x'`; for a plain string returns the string; for unknown returns the fallback sentence
    - Test (type-level / unit if testable without Tauri runtime): the store exposes `status`, `loading`, `error` getters and `loadStatus`/`setProviderKey`/`clearProviderKey` methods
  </behavior>
  <read_first>
    - src/lib/stores/history.ts (full pattern: $state runes, normalizeIpcError, invoke usage, getter exposure)
    - src/lib/stores/surface.ts (normalizeIpcError reference)
    - .planning/phases/04-privacy/04-RESEARCH.md (Pattern 8 privacyStore full TypeScript)
    - .planning/phases/04-privacy/04-UI-SPEC.md (Interaction Contract state machine, CredentialStatus values)
  </read_first>
  <action>
    Create `src/lib/stores/settings.ts` exporting a `privacyStore` singleton built by a `createPrivacyStore()` factory, following the `historyStore` pattern. Export `type CredentialStatus = 'CONFIGURED' | 'MISSING'` and `type ProviderId = 'OpenRouter'` (matching Rust serde output — confirm casing against `secrets::CredentialStatus` SCREAMING_SNAKE_CASE and `ProviderId`). Reuse the same `normalizeIpcError` shape as history.ts. State runes: `status` (default `'MISSING'`), `loading` (bool), `error` (`string | null`). Methods (each sets loading true + error null, try/catch with normalizeIpcError, finally loading false): `loadStatus(provider = 'OpenRouter')` → `invoke<CredentialStatus>('privacy_get_credential_status', { provider })`; `setProviderKey(provider, key)` → `invoke<void>('privacy_set_provider_key', { provider, key })` then set status `'CONFIGURED'`; `clearProviderKey(provider = 'OpenRouter')` → `invoke<void>('privacy_clear_provider_key', { provider })` then set status `'MISSING'`. Expose only getters for status/loading/error plus the three methods (narrowest writable surface per frontend rules). The store must NEVER cache or store the key value. Give every exported helper explicit param + return types (frontend TS baseline).
  </action>
  <verify>
    <automated>npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep -v '^#' | grep -Ec 'error|svelte-check found 0'</automated>
  </verify>
  <acceptance_criteria>
    - `settings.ts` exports `privacyStore` and types `CredentialStatus`, `ProviderId`
    - three methods call `invoke` with the exact command names `privacy_get_credential_status`, `privacy_set_provider_key`, `privacy_clear_provider_key`
    - payload keys are `provider` and (for set) `key` — matching Rust command param names
    - store exposes status/loading/error as getters only; key value is never held in store state
    - `npx svelte-check --tsconfig ./tsconfig.json` reports 0 errors for this file
  </acceptance_criteria>
  <done>privacyStore wraps the three commands with normalizeIpcError; no key caching; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SettingsSurface.svelte credential UI per UI-SPEC</name>
  <files>src/lib/components/surfaces/SettingsSurface.svelte</files>
  <read_first>
    - src/lib/components/surfaces/SettingsSurface.svelte (current scaffold to replace)
    - .planning/phases/04-privacy/04-UI-SPEC.md (FULL contract: layout structure, status indicator, input/button specs, inline confirmation, copywriting table, ARIA contract, color/typography/spacing tokens, security interaction invariants)
    - src/lib/stores/settings.ts (privacyStore API from Task 1)
    - .claude/rules/frontend.md (Svelte 5 runes, $effect cleanup, component contracts)
  </read_first>
  <action>
    Replace the scaffold `SettingsSurface.svelte` with the credential-management UI. Use `<script lang="ts">`, import `privacyStore`. On mount call `privacyStore.loadStatus()`. Render the layout from UI-SPEC §Phase 4 Component Inventory exactly: `.surface-header` with "Settings" h1 (16px/600 #e0e0e0); `.surface-body` (padding 24px) containing `section.credentials-section` (max-width 480px, gap 24px) with `h2` "API Credentials", a `.credential-card` (#242424 bg, #2a2a2a border, radius 8px, padding 16px) containing: `.provider-label` "OpenRouter"; a `.status-row` with `aria-live="polite"` showing the status indicator (dot + text per the three states — CONFIGURED `#4a9eff` "API key configured"; MISSING `#888` "No API key set"; loading pulse "Checking…"); a key entry form shown when status is MISSING with `input[type="password"]` (`autocomplete="off"`, `aria-label="API key for OpenRouter"`, placeholder "Paste your API key", visual spec height 40px / #1a1a1a / #2a2a2a border / focus #4a9eff) bound to a LOCAL `$state` string; a `.btn-save` "Save API Key" (#4a9eff bg, disabled when input empty/whitespace or loading, `aria-busy={loading}`, `aria-label="Save API key for OpenRouter"`); a `.btn-clear` "Remove key" shown only when CONFIGURED (transparent, hover #cc4444, `aria-label="Remove API key for OpenRouter"`). Remove triggers an inline confirmation (NOT modal) following the ConversationRow delete pattern: container #2a1a1a bg / #7a1e1e border, heading "Remove API key?", body copy from UI-SPEC, "Confirm remove" + "Cancel", `role="alertdialog" aria-label="Confirm key removal" aria-modal="true"`, Escape cancels and returns focus to the Remove button. Error region: `role="alert"`, rendered ONLY when `privacyStore.error` is non-null (do not render an empty live region), text 14px #cc4444. Use the exact copy strings from the Copywriting Contract table.
    SECURITY INVARIANTS (mandatory): after each `setProviderKey` call resolves (success OR failure), clear the local input `$state` to `''` immediately. The component must NEVER render the key value or any derivative back to the DOM. Input is `type="password"`. All interactive elements get the focus ring `outline: 2px solid #4a9eff; outline-offset: 2px`. Use only the color/typography/spacing tokens declared in UI-SPEC — introduce no new hex values or font sizes. Keyboard tab order per UI-SPEC §Keyboard Contract.
  </action>
  <verify>
    <human-check>
      Run the app, open Settings. Verify per UI-SPEC: (1) status shows "No API key set" with grey dot when missing; (2) typing a key enables "Save API Key", saving clears the input and flips status to "API key configured" with blue dot; (3) "Remove key" opens the inline confirmation, Confirm removes and returns to "No API key set", Cancel/Escape dismisses and returns focus to Remove; (4) a forced error shows a red role=alert message; (5) the key value never appears anywhere in the DOM/DevTools after save.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `SettingsSurface.svelte` contains `type="password"` and `autocomplete="off"` on the key input
    - local input `$state` is set to `''` after `setProviderKey` resolves in both success and catch/finally paths
    - status rendering only ever shows "API key configured" / "No API key set" / "Checking…" — no key or derivative
    - inline confirmation uses `role="alertdialog"` and `aria-modal="true"`; Escape cancels
    - error region is conditionally rendered (`{#if privacyStore.error}`) with `role="alert"`
    - copy strings match the UI-SPEC Copywriting Contract exactly (e.g. "Save API Key", "Remove key", "Remove API key?")
    - `npx svelte-check --tsconfig ./tsconfig.json` reports 0 errors for this file
  </acceptance_criteria>
  <done>SettingsSurface implements the write-only credential UI per UI-SPEC; key never rendered; input cleared after submit; type-check clean; human verification passed.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 07)

- `src/lib/stores/settings.ts` — `privacyStore`, `CredentialStatus`, `ProviderId`, `createPrivacyStore`
- `SettingsSurface.svelte` credential-management UI (replaces scaffold)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → renderer → privacy IPC | The key is held in renderer memory only momentarily, then sent once and cleared |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Information Disclosure | key leaking into DOM/DevTools | mitigate | input is type=password; key never rendered back; local $state cleared after each IPC call (UI-SPEC security invariants, D-02) |
| T-04-20 | Information Disclosure | store caching the key | mitigate | privacyStore holds only status/loading/error; never stores the key value (D-10) |
</threat_model>

<verification>
- `npx svelte-check --tsconfig ./tsconfig.json` reports 0 errors
- Human-check passes per UI-SPEC interaction + security invariants
- Key value never appears in DOM after save
</verification>

<success_criteria>
- privacyStore wraps the three commands with normalizeIpcError, no key caching
- SettingsSurface matches UI-SPEC layout/copy/ARIA; write-only key entry cleared after submit
- Type-check clean; human verification passed
</success_criteria>

<output>
Create `.planning/phases/04-privacy/04-07-SUMMARY.md` when done
</output>
