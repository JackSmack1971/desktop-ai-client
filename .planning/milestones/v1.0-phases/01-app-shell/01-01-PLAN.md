---
phase: 01-app-shell
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - svelte.config.js
  - vite.config.ts
  - tsconfig.json
  - src/app.html
  - src/routes/+layout.svelte
  - src/routes/+page.svelte
  - src/lib/components/AppShell.svelte
  - src/lib/components/SurfaceRail.svelte
  - src/lib/stores/surface.ts
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - src-tauri/src/main.rs
  - src-tauri/src/app_state.rs
  - src-tauri/src/ipc/mod.rs
  - src-tauri/src/ipc/app_shell.rs
  - src-tauri/src/storage/mod.rs
  - src-tauri/src/storage/sqlite.rs
  - src-tauri/src/storage/migrations.rs
autonomous: true
requirements:
  - SHELL-01
must_haves:
  truths:
    - The app opens into a usable desktop shell instead of a blank scaffold.
    - The shell loads a backend-owned active-surface preference on startup.
    - Frontend navigation changes the visible surface without exposing raw file paths or secrets.
  artifacts:
    - package.json
    - src-tauri/Cargo.toml
    - src-tauri/src/main.rs
    - src-tauri/src/ipc/app_shell.rs
    - src/routes/+layout.svelte
    - src/lib/components/AppShell.svelte
    - src/lib/stores/surface.ts
  key_links:
    - Tauri bootstrap initializes the shell state and window before the SvelteKit frontend renders.
    - The frontend shell reads and writes surface state through typed IPC commands, not browser storage.
    - The backend storage layer owns persistence for shell preferences.
---

<objective>
Create the runnable desktop shell foundation for Phase 1 by establishing the Tauri v2 + Svelte 5/SvelteKit build stack, wiring the backend bootstrap, and adding backend-owned persistence for the initial active surface.

Purpose: prove the app can launch from the desktop shell and restore shell state through the backend boundary.
Output: a buildable desktop app scaffold, typed shell IPC, and a persisted workspace-shell preference.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/architecture.md
@docs/implementation-plan.md
@docs/privacy-boundaries.md
@docs/threat-model.md
@docs/Tauri_Svelte_AI_App_Architecture_Adversarial_Hardened_v5.md
@src-tauri/AGENTS.md
@src-tauri/src/AGENTS.md
@src-tauri/src/ipc/AGENTS.md
@src-tauri/src/storage/AGENTS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/architecture.md
@docs/privacy-boundaries.md
@docs/threat-model.md
@docs/Tauri_Svelte_AI_App_Architecture_Adversarial_Hardened_v5.md
@src-tauri/AGENTS.md
@src-tauri/src/AGENTS.md
@src-tauri/src/ipc/AGENTS.md
@src-tauri/src/storage/AGENTS.md
</context>

<tasks>
<task type="auto">
  <name>Bootstrap the runnable app stack</name>
  <files>package.json, svelte.config.js, vite.config.ts, tsconfig.json, src/app.html, src-tauri/Cargo.toml, src-tauri/tauri.conf.json, src-tauri/src/main.rs, src-tauri/src/app_state.rs</files>
  <read_first>docs/Tauri_Svelte_AI_App_Architecture_Adversarial_Hardened_v5.md, docs/architecture.md, src-tauri/AGENTS.md, src-tauri/src/AGENTS.md</read_first>
  <action>Create the root frontend and Rust manifests for a Tauri v2 + Svelte 5/SvelteKit desktop app. Keep `app.withGlobalTauri` disabled, point the frontend dist at the built web app, and replace the scaffold-only `main.rs` with a thin bootstrap that initializes app state and registers backend commands. Scripts must include a real dev loop, a build command, and a frontend typecheck/check command.</action>
  <verify>Run `npm run check`, then `cargo test --workspace --all-targets` once the manifests and bootstrap exist.</verify>
  <acceptance_criteria>
    - `src-tauri/tauri.conf.json` launches a real app window and keeps `withGlobalTauri` false.
    - `src-tauri/src/main.rs` boots the runtime instead of containing only the scaffold comment.
    - Root package scripts exist for dev, build, and check.
  </acceptance_criteria>
  <done>The repo can build the app shell stack from the new manifests without exposing backend-owned state to the frontend.</done>
</task>

<task type="auto">
  <name>Add backend-owned shell persistence</name>
  <files>src/lib/components/AppShell.svelte, src/lib/components/SurfaceRail.svelte, src/lib/stores/surface.ts, src/routes/+layout.svelte, src/routes/+page.svelte, src-tauri/src/ipc/mod.rs, src-tauri/src/ipc/app_shell.rs, src-tauri/src/storage/mod.rs, src-tauri/src/storage/sqlite.rs, src-tauri/src/storage/migrations.rs</files>
  <read_first>docs/privacy-boundaries.md, docs/threat-model.md, src-tauri/src/ipc/AGENTS.md, src-tauri/src/storage/AGENTS.md</read_first>
  <action>Implement typed IPC for reading and setting the active surface, persist that preference through backend-owned SQLite-backed storage, and hydrate the initial workspace shell from backend state on startup. Render a visible workspace shell with chat, history, settings, and artifact surfaces driven by Svelte 5 runes rather than browser storage or raw path access.</action>
  <verify>Launch the app with `npm run dev`, switch surfaces in the shell, restart, and confirm the last surface is restored from backend persistence.</verify>
  <acceptance_criteria>
    - The first render shows a workspace shell with the four major surfaces named in the roadmap.
    - Switching surfaces updates backend-owned state through typed IPC.
    - The selected surface survives a restart without frontend-local persistence.
  </acceptance_criteria>
  <done>The shell behaves as a real desktop entry point, not a static placeholder.</done>
</task>
</tasks>

<verification>
Before declaring the plan complete:
- [ ] `npm run check` succeeds after the scaffold exists.
- [ ] `cargo test --workspace --all-targets` succeeds for the backend shell plumbing.
- [ ] The app launches into the workspace shell and restores the last active surface on relaunch.
</verification>

<success_criteria>

- The desktop app launches successfully from the desktop shell.
- The user can reach the main workspace and switch between the major surfaces.
- The shell is organized so backend and frontend boundaries remain explicit.
- The phase proves a real backend-owned read/write path, not just static UI chrome.
  </success_criteria>
