---
phase: 04-privacy
plan: 04
type: execute
wave: 3
depends_on: ['04-01', '04-02']
files_modified:
  - src-tauri/src/telemetry/audit_log.rs
  - src-tauri/src/telemetry/mod.rs
  - src-tauri/src/ipc/privacy.rs
autonomous: true
requirements: [SEC-03, SEC-01]
user_setup: []

must_haves:
  truths:
    - 'AuditEntry has only metadata fields (timestamp, command, window, status) — no content, path, or secret fields'
    - 'write_audit_entry appends one JSON line and creates the logs dir if missing'
    - 'the three privacy IPC commands never return or echo the key value'
    - 'each privacy command calls policy_check before doing work and uses deny_unknown_fields request structs'
  artifacts:
    - path: 'src-tauri/src/telemetry/audit_log.rs'
      provides: 'AuditEntry struct + write_audit_entry + tests'
      contains: 'AuditEntry'
    - path: 'src-tauri/src/ipc/privacy.rs'
      provides: 'privacy_set/get/clear commands + PrivacyError + tests'
      contains: 'privacy_set_provider_key'
  key_links:
    - from: 'src-tauri/src/ipc/privacy.rs'
      to: 'security::command_policy::policy_check'
      via: 'policy_check before work'
      pattern: 'policy_check'
    - from: 'src-tauri/src/ipc/privacy.rs'
      to: 'security::secrets::store_provider_key'
      via: 'set/clear delegate to keychain'
      pattern: 'store_provider_key|delete_provider_key'
---

<objective>
Implement the telemetry audit log (`telemetry::audit_log`) and the three privacy IPC commands (`ipc::privacy`). The audit log writes redaction-safe JSON Lines; the privacy commands provide the only credential write/clear/status surface and never expose the key value.

Purpose: This delivers the SEC-01 IPC surface and the SEC-03 audit sink together because the privacy commands are the first callers of `write_audit_entry`. Implements D-08, D-10. Depends on Plan 01 (secrets backing, deps) and Plan 02 (command_policy, redaction).
Output: audit_log.rs, telemetry/mod.rs module decl, ipc/privacy.rs + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-privacy/04-CONTEXT.md
@.planning/phases/04-privacy/04-RESEARCH.md
@.planning/phases/04-privacy/04-VALIDATION.md
@src-tauri/src/ipc/app_shell.rs
@src-tauri/src/security/secrets.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement telemetry::audit_log JSON Lines writer</name>
  <files>src-tauri/src/telemetry/audit_log.rs, src-tauri/src/telemetry/mod.rs</files>
  <behavior>
    - Test: `AuditEntry` serializes to valid JSON that round-trips via serde_json::from_str
    - Test: a serialized `AuditEntry` for command "privacy_set_provider_key" contains no path-like or secret-like substring (no "/", no "sk-")
    - Test: `AuditEntry` JSON contains exactly the metadata keys (timestamp, command, window, status) and no others
  </behavior>
  <read_first>
    - src-tauri/src/telemetry/audit_log.rs (current scaffold placeholder)
    - src-tauri/src/telemetry/mod.rs (scaffold placeholder — must add `pub mod audit_log;`; note release_evidence.rs also exists undeclared, leave it as-is unless it blocks compile)
    - src-tauri/src/main.rs (app_log_dir / create_dir_all pattern used for app_data_dir in setup hook)
    - .planning/phases/04-privacy/04-RESEARCH.md (Pattern 5 audit JSON Lines, Pitfall 6 create_dir_all, D-08/D-09, PathResolver app_log_dir example)
  </read_first>
  <action>
    In `telemetry/mod.rs` add `pub mod audit_log;` (the current file is a placeholder comment — preserve any needed existing declarations; if `release_evidence`/other modules are referenced elsewhere keep them, otherwise only ensure `audit_log` is declared and the crate compiles). In `audit_log.rs` define `pub struct AuditEntry { pub timestamp: String, pub command: String, pub window: String, pub status: String }` deriving `Debug, serde::Serialize, serde::Deserialize` — it has NO content/path/secret fields by construction (this is the structural redaction guarantee, D-09). Add `pub fn write_audit_entry(app_handle: &tauri::AppHandle, entry: AuditEntry) -> Result<(), std::io::Error>`: resolve `app_handle.path().app_log_dir()`, `std::fs::create_dir_all(&log_dir)` first (Pitfall 6), join `"audit.log"`, open with `OpenOptions::new().create(true).append(true)`, write `serde_json::to_string(&entry)` + newline via `writeln!`. Provide a constructor helper `AuditEntry::new(command: &str, window: &str, status: &str) -> Self` that fills `timestamp` from `chrono::Utc::now().to_rfc3339()`. Tests must not require a Tauri runtime — test `AuditEntry` serialization/field-set directly (do not call `write_audit_entry`, which needs an AppHandle; that path is covered by the manual-only verification in VALIDATION.md).
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml -- telemetry::audit_log::tests 2>&1 | grep -v '^#' | grep -c 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `telemetry/mod.rs` contains `pub mod audit_log;`
    - `AuditEntry` has only `timestamp`, `command`, `window`, `status` fields
    - `write_audit_entry` calls `create_dir_all` before `OpenOptions` and uses `.append(true)`
    - Test asserts serialized entry has no `/` or `sk-` substring and round-trips through serde
    - `cargo test --manifest-path src-tauri/Cargo.toml -- telemetry::audit_log::tests` exits 0
  </acceptance_criteria>
  <done>Append-only JSON Lines writer with metadata-only AuditEntry; tests green; logs dir auto-created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ipc::privacy three commands + PrivacyError</name>
  <files>src-tauri/src/ipc/privacy.rs</files>
  <behavior>
    - Test: `PrivacyError` serializes to `{ code: SCREAMING_SNAKE_CASE, message }`
    - Test: the `Result` type of `privacy_set_provider_key` is `Result<(), PrivacyError>` — success returns unit, never a key (type-level / signature assertion)
    - Test: a `SetProviderKeyRequest`-style deserialization rejects an unknown extra field (deny_unknown_fields), if a request struct is used
  </behavior>
  <read_first>
    - src-tauri/src/ipc/privacy.rs (current scaffold placeholder)
    - src-tauri/src/ipc/app_shell.rs (ShellError serde shape; #[tauri::command] async signature; assert_main_window being superseded by policy_check)
    - src-tauri/src/security/secrets.rs (store_provider_key/read?/delete_provider_key signatures, get_credential_status, ProviderId, CredentialStatus — from Plan 01)
    - src-tauri/src/security/command_policy.rs (policy_check signature — from Plan 02)
    - src-tauri/src/telemetry/audit_log.rs (`telemetry::audit_log::write_audit_entry` + `AuditEntry::new` signatures — from Task 1; read before writing the privacy.rs audit wiring)
    - .planning/phases/04-privacy/04-RESEARCH.md (Pattern 2 PrivacyError, Pattern 3 deny_unknown_fields + Pitfall 5 do-not-combine-with-tag, D-10, Anti-Patterns no key echo)
  </read_first>
  <action>
    Implement three `#[tauri::command] pub async fn`:
    `privacy_set_provider_key(window, state, provider: String, key: String) -> Result<(), PrivacyError>` — call `command_policy::policy_check("privacy_set_provider_key", window.label())?`, map the provider string to `secrets::ProviderId` (reject unknown → `PrivacyError::UnsupportedProvider`), call `secrets::store_provider_key`, return `Ok(())`. The `key` parameter must NEVER appear in any log, error message, or audit entry — construct all error strings without it.
    `privacy_get_credential_status(window, state, provider: String) -> Result<secrets::CredentialStatus, PrivacyError>` — policy_check, map provider, return `secrets::get_credential_status(...)`.
    `privacy_clear_provider_key(window, state, provider: String) -> Result<(), PrivacyError>` — policy_check, map provider, call `secrets::delete_provider_key`, return `Ok(())`.
    AUDIT WIRING (mandatory, D-08): At the end of each command handler (after the primary operation resolves, regardless of success or failure), call `write_audit_entry(&app_handle, AuditEntry { timestamp: chrono::Utc::now().to_rfc3339(), command: <command_name>, window: window.label().to_string(), status: if result is Ok then "ok" else "error" })` (or the `AuditEntry::new(command, window, status)` helper from Task 1). Never log the key value or any content — only command name, window label, and status string. The audit write happens on both the success and error paths (do not early-return before auditing); a `write_audit_entry` failure must not mask the command's own result. The command needs an `app_handle: tauri::AppHandle` parameter to resolve the log dir — add it to each command signature.
    Define `PrivacyError` enum (variants `UnauthorizedWindow(String)`, `UnsupportedProvider(String)`, `CredentialStoreError(String)`, `PolicyViolation(String)`) deriving `thiserror::Error + serde::Serialize` with `#[serde(tag="code", content="message", rename_all="SCREAMING_SNAKE_CASE")]`. Provide `From<command_policy::PolicyError>` and `From<secrets::SecretsError>` for `PrivacyError` so `?` works — these `From` impls must NOT embed the key value (SecretsError messages already exclude it per Plan 01). Do NOT add `deny_unknown_fields` to `PrivacyError` (Pitfall 5: tag+content enum). If a request struct is introduced for any command, that struct gets `#[serde(deny_unknown_fields)]` — but the key is a direct command parameter, not a struct field, so no `api_key` field exists anywhere. No command returns the key value in any variant. Tests: assert PrivacyError JSON code is SCREAMING_SNAKE_CASE; assert (via a compile-time/`fn` signature check or doc) the set command returns `Result<(), PrivacyError>`.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml -- ipc::privacy::tests 2>&1 | grep -v '^#' | grep -c 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `privacy.rs` defines all three `#[tauri::command]` functions with the names above
    - Each command calls `command_policy::policy_check(...)` before any work
    - Each command calls `telemetry::audit_log::write_audit_entry` at the end on both success and error paths, passing only command name, window label, and status ("ok"/"error") — never the key
    - `privacy_set_provider_key` returns `Result<(), PrivacyError>` (no key in success or error)
    - `PrivacyError` uses `#[serde(tag="code", content="message", rename_all="SCREAMING_SNAKE_CASE")]` and has no `deny_unknown_fields`
    - `grep -v '^#' src-tauri/src/ipc/privacy.rs | grep -c 'key'` — manual confirm `key` never appears inside a `format!`/error/log/audit construction
    - No response type contains a key/secret field
    - `cargo test --manifest-path src-tauri/Cargo.toml -- ipc::privacy::tests` exits 0
  </acceptance_criteria>
  <done>Three privacy commands enforce policy, audit on every path, never expose the key, typed PrivacyError; tests green.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (Plan 04)

- `telemetry::audit_log::AuditEntry` struct + `AuditEntry::new`
- `telemetry::audit_log::write_audit_entry` fn
- `telemetry/mod.rs` `pub mod audit_log;` declaration
- `ipc::privacy::privacy_set_provider_key` command
- `ipc::privacy::privacy_get_credential_status` command
- `ipc::privacy::privacy_clear_provider_key` command
- `ipc::privacy::PrivacyError` enum
- Tests: `telemetry::audit_log::tests::*`, `ipc::privacy::tests::*`
  </artifacts_produced>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                       |
| ---------------------- | ----------------------------------------------------------------- |
| renderer → privacy IPC | Renderer may write/clear/query credential; may never read the key |
| backend → audit.log    | Only metadata crosses into the persisted audit trail              |

## STRIDE Threat Register

| Threat ID | Category               | Component                               | Disposition | Mitigation Plan                                                                                |
| --------- | ---------------------- | --------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------- |
| T-04-09   | Information Disclosure | frontend reading key via IPC response   | mitigate    | no privacy command returns the key; status is CredentialStatus enum only (D-10)                |
| T-04-10   | Information Disclosure | key value in audit log                  | mitigate    | AuditEntry has no content/secret fields by construction; key never passed to write path (D-08) |
| T-04-11   | Tampering              | extra-parameter smuggling               | mitigate    | key is a typed command param, no api_key field; any request struct uses deny_unknown_fields    |
| T-04-12   | Spoofing               | non-main window calling privacy command | mitigate    | policy_check enforced first in every command                                                   |

</threat_model>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml -- telemetry::audit_log::tests ipc::privacy::tests` green
- No privacy command signature or error variant carries the key value
</verification>

<success_criteria>

- AuditEntry metadata-only; write_audit_entry appends JSON Lines with dir creation
- Three privacy commands enforce policy_check, audit on every path, and never expose the key
- Wave 0 audit_log + privacy tests green
  </success_criteria>

<output>
Create `.planning/phases/04-privacy/04-04-SUMMARY.md` when done
</output>
