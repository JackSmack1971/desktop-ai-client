---
phase: 04-privacy
plan: 02
type: execute
wave: 2
depends_on: ['04-01']
files_modified:
  - src-tauri/src/security/redaction.rs
  - src-tauri/src/security/command_policy.rs
autonomous: true
requirements: [SEC-03, SEC-01]
user_setup: []

must_haves:
  truths:
    - 'redact_secret, redact_path, redact_content each replace their target with [REDACTED]'
    - 'command_policy.policy_check allows correct (command, window) pairs and denies wrong window labels'
    - 'command_policy denies commands not present in the static allow-table (deny-by-default)'
    - 'command_policy is structured so Phase 6 can swap the backing table without changing call sites'
  artifacts:
    - path: 'src-tauri/src/security/redaction.rs'
      provides: 'redact_secret/redact_path/redact_content + tests'
      contains: 'REDACTED'
    - path: 'src-tauri/src/security/command_policy.rs'
      provides: 'CommandPolicy struct + policy_check() + PolicyError + tests'
      contains: 'policy_check'
  key_links:
    - from: 'src-tauri/src/security/command_policy.rs'
      to: 'static allow-table'
      via: 'CommandPolicy::new() const table'
      pattern: 'policy_check'
---

<objective>
Implement two independent, runtime-free security primitives: `security::redaction` (the unconditional redaction gate for the three sensitive categories) and `security::command_policy` (the static window-label allow-table replacing per-handler `assert_main_window`).

Purpose: These are pure functions with no Tauri runtime dependency, so they are fully unit-testable in Wave 0 and consumed by the IPC + audit-log plans in Wave 3. Implements D-03, D-07, D-09.
Output: redaction.rs and command_policy.rs with full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-privacy/04-CONTEXT.md
@.planning/phases/04-privacy/04-RESEARCH.md
@.planning/phases/04-privacy/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement security::redaction with the three categories</name>
  <files>src-tauri/src/security/redaction.rs</files>
  <behavior>
    - Test: `redact_secret` of any non-empty key string returns the literal `[REDACTED]` (never the original value or a prefix)
    - Test: `redact_path` of `/home/user/secret.txt` returns `[REDACTED]` (no path segment leaks)
    - Test: `redact_content` of arbitrary prompt text returns `[REDACTED]`
    - Test: redaction is unconditional — the same input redacts identically regardless of any build flag (no debug escape hatch)
  </behavior>
  <read_first>
    - src-tauri/src/security/redaction.rs (current scaffold placeholder)
    - .planning/phases/04-privacy/04-RESEARCH.md (D-07/D-09 redaction categories, Anti-Patterns "Never log or echo", Security Domain threat patterns)
    - src-tauri/src/ipc/app_shell.rs (module doc-comment style to match)
  </read_first>
  <action>
    Implement three free functions in `redaction.rs`: `pub fn redact_secret(_value: &str) -> String`, `pub fn redact_path(_path: &std::path::Path) -> String` (accept `&Path` so callers pass a `PathBuf`/`&Path` directly), and `pub fn redact_content(_content: &str) -> String`. Each returns the constant `"[REDACTED]"` (define a `const REDACTED: &str = "[REDACTED]"`). Redaction is total replacement, not masking — do not return partial values, lengths, or hashes that could leak structure (D-09: no debug/dev escape hatch lowers redaction). Add a module doc comment stating these are the unconditional gate before any log/telemetry write for the three D-07 categories (secrets, paths, content). Include a `#[cfg(test)] mod tests` covering each function plus an assertion that a known secret-like string `"sk-or-v1-abc123"` does not appear as a substring of `redact_secret`'s output.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml -- security::redaction::tests 2>&1 | grep -v '^#' | grep -c 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `redaction.rs` defines `redact_secret`, `redact_path`, `redact_content` returning `[REDACTED]`
    - `redact_path` accepts `&std::path::Path`
    - Test asserts `"sk-or-v1-abc123"` is not a substring of `redact_secret(...)` output
    - No build-flag branch alters redaction output
    - `cargo test --manifest-path src-tauri/Cargo.toml -- security::redaction::tests` exits 0
  </acceptance_criteria>
  <done>Three redaction functions implemented; total replacement verified; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement security::command_policy allow-table</name>
  <files>src-tauri/src/security/command_policy.rs</files>
  <behavior>
    - Test: `policy_check("privacy_set_provider_key", "main")` returns `Ok(())`
    - Test: `policy_check("privacy_set_provider_key", "evil")` returns `Err(PolicyError::UnauthorizedWindow(_))`
    - Test: `policy_check("nonexistent_command", "main")` returns `Err(PolicyError::UnknownCommand(_))` (deny-by-default)
    - Test: `PolicyError` serializes to `{ code: SCREAMING_SNAKE_CASE, message }`
  </behavior>
  <read_first>
    - src-tauri/src/security/command_policy.rs (current scaffold placeholder)
    - .planning/phases/04-privacy/04-RESEARCH.md (Pattern 6 CommandPolicy full code, D-03 Phase 6 swap requirement, Open Question 3 migration scope)
    - src-tauri/src/ipc/app_shell.rs (ShellError serde shape to mirror for PolicyError)
    - src-tauri/src/main.rs (existing registered command names to include in the table)
  </read_first>
  <action>
    Implement `CommandPolicy` with a `table: &'static [(&'static str, &'static [&'static str])]` field, a `pub const fn new()` constructor populating the static table, and a `pub fn check(&self, command: &str, window_label: &str) -> Result<(), PolicyError>`. Expose `pub static POLICY: CommandPolicy = CommandPolicy::new();` and a free `pub fn policy_check(command: &str, window_label: &str) -> Result<(), PolicyError>` delegating to `POLICY.check(...)` — this free-function indirection is the Phase 6 swap seam (D-03: replace the table, not the call sites). Populate the table with the 5 new Phase 4 commands (`privacy_set_provider_key`, `privacy_get_credential_status`, `privacy_clear_provider_key`, `files_open_dialog`, `files_read_token`) each allowed for `["main"]`, plus the existing commands (`get_active_surface`, `set_active_surface`, `chat_send`, `chat_cancel`, `history_list`, `history_get`, `history_delete`, `history_search`) allowed for `["main"]`. Deny-by-default: a command absent from the table returns `PolicyError::UnknownCommand`. Define `PolicyError` enum (variants `UnauthorizedWindow(String)`, `UnknownCommand(String)`) deriving `thiserror::Error + serde::Serialize` with `#[serde(tag = "code", content = "message", rename_all = "SCREAMING_SNAKE_CASE")]` mirroring `ShellError`. Do NOT combine `deny_unknown_fields` on this enum (Pitfall 5). Per Open Question 3: do not migrate existing handlers' `assert_main_window` calls in this plan — only define the policy module; new Phase 4 handlers will call it in Wave 3.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml -- security::command_policy::tests 2>&1 | grep -v '^#' | grep -c 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `command_policy.rs` contains `pub fn policy_check(` and `pub static POLICY`
    - Table includes all 5 Phase 4 command names, each mapped to `["main"]`
    - `policy_check` returns `UnknownCommand` for absent command, `UnauthorizedWindow` for wrong label, `Ok` for correct pair
    - `PolicyError` uses `#[serde(tag = "code", content = "message", rename_all = "SCREAMING_SNAKE_CASE")]` and has no `deny_unknown_fields`
    - Test confirms a `PolicyError` JSON contains a SCREAMING_SNAKE_CASE code
    - `cargo test --manifest-path src-tauri/Cargo.toml -- security::command_policy::tests` exits 0
  </acceptance_criteria>
  <done>Static allow-table enforces window labels with deny-by-default; Phase 6 swap seam present; tests green.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (Plan 02)

- `redaction::redact_secret`, `redaction::redact_path`, `redaction::redact_content` fns
- `redaction::REDACTED` const
- `command_policy::CommandPolicy` struct + `CommandPolicy::new`/`check`
- `command_policy::POLICY` static
- `command_policy::policy_check` fn
- `command_policy::PolicyError` enum (`UnauthorizedWindow`, `UnknownCommand`)
- Tests: `security::redaction::tests::*`, `security::command_policy::tests::*`
  </artifacts_produced>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                              |
| ------------------------------ | ------------------------------------------------------------------------ |
| backend logic → telemetry sink | Sensitive values must be redacted before any persisted/transmitted write |
| renderer window → IPC command  | Only allow-listed (command, window) pairs may execute                    |

## STRIDE Threat Register

| Threat ID | Category               | Component                                      | Disposition | Mitigation Plan                                                                 |
| --------- | ---------------------- | ---------------------------------------------- | ----------- | ------------------------------------------------------------------------------- |
| T-04-03   | Information Disclosure | log/telemetry writes                           | mitigate    | redact_secret/path/content total-replace; unconditional, no debug escape (D-09) |
| T-04-04   | Spoofing               | unauthorized window calling privileged command | mitigate    | command_policy.policy_check enforces window-label allow-table (D-03)            |
| T-04-05   | Elevation of Privilege | unlisted command reachable                     | mitigate    | deny-by-default — absent command returns UnknownCommand                         |

</threat_model>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml -- security::redaction::tests security::command_policy::tests` all green
- redact functions return only `[REDACTED]`; no input substring leaks
</verification>

<success_criteria>

- redaction.rs covers all three D-07 categories with total replacement
- command_policy.rs enforces allow-table with deny-by-default and Phase 6 swap seam
- Wave 0 tests for both modules green
  </success_criteria>

<output>
Create `.planning/phases/04-privacy/04-02-SUMMARY.md` when done
</output>
