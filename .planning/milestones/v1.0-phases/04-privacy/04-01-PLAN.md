---
phase: 04-privacy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/app_state.rs
  - src-tauri/src/security/secrets.rs
autonomous: true
requirements: [SEC-01]
user_setup: []

must_haves:
  truths:
    - 'get_provider_key returns NotConfigured when the OS keychain has no entry (production fail-closed)'
    - 'store_provider_key writes to the OS keychain; read returns that value (mock-backed in test)'
    - 'EnvSecretStore is reachable only behind an explicit dev/test cfg flag, never in production default path'
    - 'AppState carries a file_tokens map that starts empty and drops on quit'
  artifacts:
    - path: 'src-tauri/Cargo.toml'
      provides: 'keyring v3, mime_guess, tauri-plugin-dialog deps + dev/test feature flag'
      contains: 'keyring'
    - path: 'src-tauri/src/app_state.rs'
      provides: 'file_tokens: Mutex<HashMap<Uuid, PathBuf>> field on AppState'
      contains: 'file_tokens'
    - path: 'src-tauri/src/security/secrets.rs'
      provides: 'KeyringSecretStore backing + store_provider_key/delete_provider_key + tests'
      contains: 'keyring::Entry'
  key_links:
    - from: 'src-tauri/src/security/secrets.rs'
      to: 'keyring::Entry'
      via: 'Entry::new(SERVICE, provider)'
      pattern: 'Entry::new'
    - from: 'src-tauri/src/app_state.rs'
      to: 'uuid::Uuid'
      via: 'file_tokens HashMap key type'
      pattern: 'HashMap<Uuid'
---

<objective>
Establish the Phase 4 foundation: add the three new Cargo dependencies (with a CI-safe dev/test flag), extend `AppState` with the in-memory `file_tokens` map, and replace the secrets backing store with `KeyringSecretStore` while keeping the locked caller interface (`get_provider_key`, `get_credential_status`) unchanged.

Purpose: Every other Phase 4 module depends on these shared files (Cargo.toml, app_state.rs) and on the keychain-backed secrets store. Doing this first prevents file-ownership conflicts and unblocks all downstream waves. Implements D-01, D-02, D-05.
Output: Updated Cargo.toml, app_state.rs with file_tokens, secrets.rs with keyring backing + Wave 0 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-privacy/04-CONTEXT.md
@.planning/phases/04-privacy/04-RESEARCH.md
@.planning/phases/04-privacy/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Cargo dependencies + dev/test secrets flag</name>
  <files>src-tauri/Cargo.toml</files>
  <read_first>
    - src-tauri/Cargo.toml (current deps + [features] block — `default = ["custom-protocol"]`)
    - .planning/phases/04-privacy/04-RESEARCH.md §Alternative: `keyring` v3 section and §Recommendation note before editing Cargo.toml — use the recommended `keyring = "3"` approach.
    - .planning/phases/04-privacy/04-RESEARCH.md (Standard Stack table, Pitfall 2 keyring v3 vs v4, Pattern 1 feature flags)
  </read_first>
  <action>
    Add to `[dependencies]`: Use `keyring = { version = "3", features = ["apple-native", "windows-native", "sync-secret-service"] }` per RESEARCH.md Recommendation (Alternative section). The Standard Stack table lists keyring-core as a forward-looking option; this phase uses keyring v3 for simplicity. Do NOT add keyring-core or platform-specific crates. (NOT v4 — v4 is sample code only per Pitfall 2; NOT bare v3 without features.) Also add `tauri-plugin-dialog = "2"` (use "2", resolves to 2.7.x), and `mime_guess = "2"`. `uuid`, `secrecy`, `serde_json`, `chrono`, `thiserror` are already present — do not re-add. Add a new feature to `[features]` named `dev-env-secrets` (default off) that gates the `EnvSecretStore` fallback path used by `secrets.rs` for CI/dev. Do NOT add `dev-env-secrets` to the `default` feature list — production builds must not include it. Keep `default = ["custom-protocol"]` unchanged otherwise.
  </action>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | grep -v '^#' | grep -ci 'error\[' ; cargo metadata --manifest-path src-tauri/Cargo.toml --format-version 1 --no-deps >/dev/null 2>&1 && echo metadata-ok</automated>
  </verify>
  <acceptance_criteria>
    - Use `keyring = { version = "3", features = ["apple-native", "windows-native", "sync-secret-service"] }` per RESEARCH.md Recommendation (Alternative section). The Standard Stack table lists keyring-core as a forward-looking option; this phase uses keyring v3 for simplicity. Do NOT add keyring-core or platform-specific crates.
    - `src-tauri/Cargo.toml` contains `keyring = { version = "3"` with `features = [` including `"windows-native"`
    - `src-tauri/Cargo.toml` contains `tauri-plugin-dialog = "2"`
    - `src-tauri/Cargo.toml` contains `mime_guess = "2"`
    - `[features]` block contains a `dev-env-secrets` entry; the `default` array does NOT contain `dev-env-secrets`
    - `cargo metadata --manifest-path src-tauri/Cargo.toml` exits 0 (manifest parses)
  </acceptance_criteria>
  <done>Three new crates resolve; `dev-env-secrets` feature exists and is off by default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend AppState with file_tokens + update test helper</name>
  <files>src-tauri/src/app_state.rs</files>
  <behavior>
    - Test: `AppState::default()` produces an empty `file_tokens` map (lock, assert is_empty)
    - Test: existing `app_state_initializes_active_requests_empty` still passes (no regression)
  </behavior>
  <read_first>
    - src-tauri/src/app_state.rs (AppState struct, Default impl, existing tests)
    - .planning/phases/04-privacy/04-RESEARCH.md (Pattern 7 AppState extension, Pitfall 4 test helper, lock ordering note)
  </read_first>
  <action>
    Add field `pub file_tokens: Mutex<std::collections::HashMap<uuid::Uuid, std::path::PathBuf>>` to `AppState` (placed after `secrets`). Initialize it in `impl Default for AppState` as `Mutex::new(HashMap::new())`. Document with a doc comment: session-scoped, in-memory only, never persisted, dropped on quit (D-05); lock is independent of shell/sqlite ordering and must never be held across an await. Add a unit test `app_state_initializes_file_tokens_empty` asserting the map starts empty. NOTE: `make_state_with_secrets()` lives in `secrets.rs`, not here — it is updated in Task 3, which is in the same plan/file set.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml -- app_state::tests 2>&1 | grep -v '^#' | grep -c 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/src/app_state.rs` contains `pub file_tokens: Mutex<` with `HashMap<uuid::Uuid` (or `Uuid`) and `PathBuf`
    - `impl Default for AppState` initializes `file_tokens: Mutex::new(HashMap::new())`
    - Test `app_state_initializes_file_tokens_empty` exists and passes
    - `cargo test --manifest-path src-tauri/Cargo.toml -- app_state::tests` exits 0
  </acceptance_criteria>
  <done>AppState carries file_tokens; default is empty; existing tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace secrets backing with KeyringSecretStore + Wave 0 tests</name>
  <files>src-tauri/src/security/secrets.rs</files>
  <behavior>
    - Test: `read_provider_key("openrouter")` on an empty mock store returns `SecretsError::NotConfigured`
    - Test: after `store_provider_key("openrouter", "sk-test")`, `read_provider_key` returns a SecretString whose exposed value equals "sk-test" (mock keyring backend in `#[cfg(test)]`)
    - Test: `delete_provider_key` on an absent entry returns `Ok(())` (idempotent)
    - Test: `get_credential_status` reflects Configured/Missing against the keychain-backed store
  </behavior>
  <read_first>
    - src-tauri/src/security/secrets.rs (LOCKED interface: ProviderId, SecretsError, CredentialStatus, get_provider_key, get_credential_status; existing make_state_with_secrets helper + tests)
    - src-tauri/src/app_state.rs (SecretsState struct, AppState fields after Task 2)
    - .planning/phases/04-privacy/04-RESEARCH.md (Pattern 1 KeyringSecretStore, Code Examples keyring v3 Entry API, Pitfall 1 delete_credential, keyring mock note in Validation Architecture)
  </read_first>
  <action>
    Add backing-store functions to `secrets.rs`: `store_provider_key(provider: &str, key: &str) -> Result<(), SecretsError>`, `read_provider_key(provider: &str) -> Result<secrecy::SecretString, SecretsError>`, and `delete_provider_key(provider: &str) -> Result<(), SecretsError>` using `keyring::Entry::new("desktop-ai-client", provider)` with `.set_password()`, `.get_password()`, `.delete_credential()` (NOT `delete_password` — v2 API per Pitfall 1). Map `keyring::Error::NoEntry` on read to `SecretsError::NotConfigured`; map it on delete to `Ok(())` (idempotent). Add a `SecretsError::StorageError(String)` variant for other keyring failures — its message must NEVER contain the key value. Wire `get_provider_key` / `get_credential_status` to consult the keychain (production fail-closed: any keychain access failure surfaces as `NotConfigured` per D-01), keeping their existing signatures unchanged. Gate the existing env-var path behind `#[cfg(any(test, feature = "dev-env-secrets"))]` so production never silently falls back to env (D-02). For CI without a live keychain: in `#[cfg(test)]`, set the keyring mock store via `keyring::set_default_credential_builder(keyring::mock::default_credential_builder())` in a test setup before exercising store/read/delete. Update `make_state_with_secrets()` to include `file_tokens: Mutex::new(HashMap::new())` in the `AppState { .. }` literal (Pitfall 4). Never call `.expose_secret()` inside any log macro, error string, or response field.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml -- secrets::tests 2>&1 | grep -v '^#' | grep -c 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `secrets.rs` contains `keyring::Entry::new(` and `.delete_credential(` (not `.delete_password(`)
    - `read_provider_key` returns `SecretsError::NotConfigured` on `keyring::Error::NoEntry`
    - env-var path is behind `#[cfg(any(test, feature = "dev-env-secrets"))]`
    - `make_state_with_secrets` includes `file_tokens:`
    - New tests cover: not-configured, store-then-read round trip, idempotent delete, status reflect
    - `cargo test --manifest-path src-tauri/Cargo.toml -- secrets::tests` exits 0
    - `grep -v '^#' src-tauri/src/security/secrets.rs | grep -c 'expose_secret()' ` shows no expose_secret call inside any `format!`/`error`/log context (manual confirm: only inside the get_provider_key clone path)
  </acceptance_criteria>
  <done>KeyringSecretStore backs secrets; locked interface unchanged; production fail-closed; mock-backed tests green.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (Plan 01)

New symbols/files created by this plan (exclude from drift verification):

- `AppState.file_tokens` field (`src-tauri/src/app_state.rs`)
- `secrets::store_provider_key` fn (`src-tauri/src/security/secrets.rs`)
- `secrets::read_provider_key` fn
- `secrets::delete_provider_key` fn
- `SecretsError::StorageError` variant
- Cargo deps: `keyring`, `tauri-plugin-dialog`, `mime_guess`
- Cargo feature: `dev-env-secrets`
- Tests: `app_state_initializes_file_tokens_empty`, `secrets::tests::*` keyring-backed cases
  </artifacts_produced>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                                          |
| ------------------------ | ------------------------------------------------------------------------------------ |
| frontend → IPC → secrets | Untrusted renderer requests credential operations; key value must stay backend-owned |
| backend → OS keychain    | Credentials persisted via platform keychain; never serialized to logs                |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                 | Disposition | Mitigation Plan                                                                                                        |
| --------- | ---------------------- | --------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-04-01   | Information Disclosure | secrets.rs error/log paths                                | mitigate    | `SecretsError::StorageError(String)` messages constructed without key value; never `.expose_secret()` in format!/log   |
| T-04-02   | Tampering              | env-var fallback in production                            | mitigate    | env path gated behind `#[cfg(any(test, feature = "dev-env-secrets"))]`; production fail-closed to NotConfigured (D-01) |
| T-04-SC   | Tampering              | cargo installs (keyring, tauri-plugin-dialog, mime_guess) | mitigate    | RESEARCH.md Package Legitimacy Audit verified all three against crates.io; all Approved, none [ASSUMED]/[SUS]          |

</threat_model>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml -- secrets::tests app_state::tests` all green
- `cargo metadata --manifest-path src-tauri/Cargo.toml` exits 0
- Production default feature set does not include `dev-env-secrets`
</verification>

<success_criteria>

- Three new crates added with correct keyring v3 features
- `AppState.file_tokens` present, empty by default
- KeyringSecretStore backs secrets with production fail-closed behavior; locked interface unchanged
- Wave 0 secrets tests (mock-backed) green
  </success_criteria>

<output>
Create `.planning/phases/04-privacy/04-01-SUMMARY.md` when done
</output>
