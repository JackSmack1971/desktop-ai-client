---
phase: 04-privacy
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src-tauri/src/security/file_tokens.rs
autonomous: true
requirements: [SEC-02]
user_setup: []

must_haves:
  truths:
    - "mint_token inserts a path under a fresh UUID and returns that token id"
    - "resolve_token returns the path for a live token and an error/None for an unknown or revoked token"
    - "revoke_token removes the token so a subsequent resolve fails"
    - "the file_tokens lock is acquired, mutated, and dropped without being held across an await"
  artifacts:
    - path: "src-tauri/src/security/file_tokens.rs"
      provides: "mint_token/resolve_token/revoke_token against AppState.file_tokens + tests"
      contains: "mint_token"
  key_links:
    - from: "src-tauri/src/security/file_tokens.rs"
      to: "AppState.file_tokens"
      via: "state.file_tokens.lock()"
      pattern: "file_tokens.lock"
---

<objective>
Implement `security::file_tokens` — the session-scoped opaque-token system operating against `AppState.file_tokens: Mutex<HashMap<Uuid, PathBuf>>`. The backend mints UUID tokens for OS-selected paths; the frontend never holds raw paths.

Purpose: This is the path-authority core of SEC-02. It is consumed by `ipc::files` in Wave 3. Pure logic against the map (no Tauri dialog runtime), so it is fully unit-testable in Wave 0. Implements D-04, D-05, D-06.
Output: file_tokens.rs with mint/resolve/revoke + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-privacy/04-CONTEXT.md
@.planning/phases/04-privacy/04-RESEARCH.md
@.planning/phases/04-privacy/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement mint/resolve/revoke against AppState.file_tokens</name>
  <files>src-tauri/src/security/file_tokens.rs</files>
  <behavior>
    - Test: `mint_token(&state, path)` returns a `Uuid`; the map then contains exactly that entry
    - Test: `resolve_token(&state, id)` returns the original `PathBuf` for a minted token
    - Test: `resolve_token` for a random unknown `Uuid` returns the not-found error/None branch
    - Test: after `revoke_token(&state, id)`, `resolve_token(&state, id)` no longer returns the path
    - Test: two mints produce two distinct token ids and two map entries
  </behavior>
  <read_first>
    - src-tauri/src/security/file_tokens.rs (current scaffold placeholder)
    - src-tauri/src/app_state.rs (file_tokens field added in Plan 01 — exact type Mutex<HashMap<Uuid, PathBuf>>)
    - src-tauri/src/security/secrets.rs (function-signature + lock-handling style: lock, operate, drop before return)
    - .planning/phases/04-privacy/04-RESEARCH.md (D-05 token map, Anti-Pattern "Never hold the file_tokens Mutex across await", Pattern 7 lock independence)
  </read_first>
  <action>
    Implement three free functions taking `state: &crate::app_state::AppState`:
    `pub fn mint_token(state: &AppState, path: std::path::PathBuf) -> Result<uuid::Uuid, FileTokenError>` — generate `uuid::Uuid::new_v4()`, lock `state.file_tokens`, insert `(id, path)`, drop the guard, return the id.
    `pub fn resolve_token(state: &AppState, token: uuid::Uuid) -> Result<std::path::PathBuf, FileTokenError>` — lock, clone the path out (so the guard can drop), return `FileTokenError::NotFound(token.to_string())` if absent.
    `pub fn revoke_token(state: &AppState, token: uuid::Uuid) -> Result<(), FileTokenError>` — lock, `remove`, drop; return `Ok(())` even if the token was already absent (idempotent).
    Define `FileTokenError` enum (`NotFound(String)`, `LockPoisoned(String)`) deriving `thiserror::Error + serde::Serialize` with the `#[serde(tag="code", content="message", rename_all="SCREAMING_SNAKE_CASE")]` shape (mirror `SecretsError`). Map a poisoned lock to `LockPoisoned`. Every function must lock, operate, and drop the guard BEFORE returning — never hold across any await (these are sync fns, which makes that structural). The returned/stored paths must never be exposed to error messages: `NotFound` carries only the token id string, never a path. Add `#[cfg(test)] mod tests` building an `AppState::default()` and exercising mint→resolve→revoke→resolve-fails plus the distinct-ids case.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml -- security::file_tokens::tests 2>&1 | grep -v '^#' | grep -c 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `file_tokens.rs` defines `mint_token`, `resolve_token`, `revoke_token` taking `&AppState`
    - `mint_token` uses `uuid::Uuid::new_v4()` and inserts into `state.file_tokens`
    - `resolve_token` returns `FileTokenError::NotFound` for unknown tokens; `NotFound` payload is the token id, never a path
    - `revoke_token` is idempotent (Ok on absent token)
    - `FileTokenError` uses the SCREAMING_SNAKE_CASE `{code,message}` serde shape
    - Tests cover mint→resolve→revoke→resolve-fails and distinct-ids
    - `cargo test --manifest-path src-tauri/Cargo.toml -- security::file_tokens::tests` exits 0
  </acceptance_criteria>
  <done>Token mint/resolve/revoke operate against AppState.file_tokens with no path leakage; lock never held across await; tests green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- `file_tokens::mint_token` fn
- `file_tokens::resolve_token` fn
- `file_tokens::revoke_token` fn
- `file_tokens::FileTokenError` enum (`NotFound`, `LockPoisoned`)
- Tests: `security::file_tokens::tests::*`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → IPC → file_tokens | Renderer holds only opaque UUIDs; raw paths stay in the backend map |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Elevation of Privilege | frontend supplying raw path | mitigate | API takes/returns only `Uuid`; paths live solely in AppState.file_tokens (D-04) |
| T-04-07 | Information Disclosure | path leaking via error string | mitigate | `FileTokenError::NotFound` carries token id only, never a path |
| T-04-08 | Denial of Service | lock held across await deadlock | mitigate | sync fns lock-operate-drop; never held across await (lock independence per Pattern 7) |
</threat_model>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml -- security::file_tokens::tests` green
- No path appears in any FileTokenError variant
</verification>

<success_criteria>
- mint/resolve/revoke implemented against AppState.file_tokens
- Tokens session-scoped, in-memory only, idempotent revoke
- Wave 0 file_tokens tests green
</success_criteria>

<output>
Create `.planning/phases/04-privacy/04-03-SUMMARY.md` when done
</output>
