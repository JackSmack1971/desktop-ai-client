---
phase: 05-artifacts
plan: "03"
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/lib/components/surfaces/ArtifactsSurface.svelte
  - src/lib/components/surfaces/ChatSurface.svelte
  - src/lib/stores/artifacts.ts
  - src-tauri/src/ipc/chat.rs
  - src-tauri/src/security/artifact_sandbox.rs
autonomous: true
requirements:
  - ARTF-03
must_haves:
  truths:
    - "Keyboard users can reach reload and stop without a mouse"
    - "The artifact preview exposes clear status feedback through live regions and an accessible title"
    - "Error and loading states are announced and fail closed instead of rendering fallback content"
    - "Focus management stays in the host UI when the preview is dismissed or reloaded"
    - "The accessibility contract matches the UI-SPEC strings, roles, and focus rings exactly"
  artifacts:
    - path: "src/lib/components/surfaces/ArtifactsSurface.svelte"
      provides: "aria-labelled host chrome, live regions, and fail-closed preview states"
      contains: "role=\"toolbar\""
    - path: "src/lib/components/surfaces/ChatSurface.svelte"
      provides: "accessible artifact-ready indicator"
      contains: "aria-live=\"polite\""
    - path: "src/lib/stores/artifacts.ts"
      provides: "state transitions for loading/error/dismissed/focus-friendly updates"
      contains: "loading"
  key_links:
    - from: "src/lib/components/surfaces/ArtifactsSurface.svelte"
      to: "src/lib/stores/artifacts.ts"
      via: "state-driven rendering for idle/loading/ready/error"
      pattern: "artifactState"
    - from: "src/lib/components/surfaces/ChatSurface.svelte"
      to: "src/lib/stores/surface.ts"
      via: "surfaceStore.setSurface('artifacts')"
      pattern: "setSurface('artifacts')"
---

<objective>
Finish the Phase 5 accessibility and fail-closed contract for artifact previews: ensure the preview surface remains keyboard-friendly, announces state changes clearly, and never falls back to unsafe rendering when preview data or CSP wrapping is unavailable.

Purpose: make the artifact experience usable under hostile content and confirm the preview chrome remains operable for keyboard and assistive-technology users.
Output: refined surface accessibility, live-region behavior, fail-closed preview handling, and verification evidence.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-artifacts/05-CONTEXT.md
@.planning/phases/05-artifacts/05-UI-SPEC.md
@docs/Tauri_Svelte_AI_App_Architecture_Adversarial_Hardened_v5.md
@docs/privacy-boundaries.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-artifacts/05-CONTEXT.md
@.planning/phases/05-artifacts/05-UI-SPEC.md
@src/lib/components/surfaces/ArtifactsSurface.svelte
@src/lib/components/surfaces/ChatSurface.svelte
@src/lib/stores/artifacts.ts
</context>

<tasks>
<task type="auto">
  <name>Polish artifact accessibility and status feedback</name>
  <files>src/lib/components/surfaces/ArtifactsSurface.svelte, src/lib/components/surfaces/ChatSurface.svelte, src/lib/stores/artifacts.ts</files>
  <read_first>
    - src/lib/components/surfaces/ArtifactsSurface.svelte
    - src/lib/components/surfaces/ChatSurface.svelte
    - src/lib/stores/artifacts.ts
    - .planning/phases/05-artifacts/05-UI-SPEC.md
  </read_first>
  <action>
    Add the accessibility details from UI-SPEC exactly: focus-visible rings, toolbar semantics, screen-reader labels, loading/error live regions, and the visible status copy for idle/ready/dismissed states. Keep the control chrome outside the sandbox and make sure focus returns to host controls after stop/dismiss transitions.
  </action>
  <verify>
    <automated>npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep -v '^#' | grep -Ec 'error|svelte-check found 0'</automated>
  </verify>
  <acceptance_criteria>
    - toolbar, buttons, iframe, and status regions carry the exact roles/labels from UI-SPEC
    - all interactive elements have a visible focus ring
    - loading and error states use the prescribed live-region behavior
  </acceptance_criteria>
  <done>The artifact preview is keyboard- and screen-reader-friendly in the host shell.</done>
</task>

<task type="auto">
  <name>Validate fail-closed preview behavior and recovery paths</name>
  <files>src/lib/components/surfaces/ArtifactsSurface.svelte, src-tauri/src/security/artifact_sandbox.rs</files>
  <read_first>
    - src/lib/components/surfaces/ArtifactsSurface.svelte
    - src-tauri/src/security/artifact_sandbox.rs
    - .planning/phases/05-artifacts/05-CONTEXT.md
    - .planning/phases/05-artifacts/05-DISCUSSION-LOG.md
  </read_first>
  <action>
    Confirm the preview is rendered only when sanitized `srcdoc` data exists, and that fetch or CSP failures replace the iframe with an explicit error state instead of a fallback document. Verify that reload re-runs backend sanitization and that stop clears the preview without leaving the host UI in a broken state.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml -- security::tests 2>&1 | grep -v '^#' | grep -c 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - the iframe is removed from the DOM in error and idle states
    - reloading always asks the backend for a fresh sanitized preview
    - stop is recoverable and never freezes the shell
  </acceptance_criteria>
  <done>The preview path fails closed and recovers through the host controls only.</done>
</task>

<task type="auto">
  <name>Run phase-5 verification and closeout checks</name>
  <files>.planning/phases/05-artifacts/05-*.md</files>
  <read_first>
    - .planning/phases/05-artifacts/05-01-PLAN.md
    - .planning/phases/05-artifacts/05-02-PLAN.md
    - .planning/phases/05-artifacts/05-CONTEXT.md
    - .planning/phases/05-artifacts/05-UI-SPEC.md
  </read_first>
  <action>
    Execute the narrow verification loop for Phase 5: run the frontend typecheck, the relevant backend tests, and a manual host-shell check of the artifact states and navigation affordance. Capture any remaining gaps as explicit follow-up items instead of silently accepting them.
  </action>
  <verify>
    <automated>npx svelte-check --tsconfig ./tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Phase 5 verification evidence covers the artifact preview, reload/stop controls, and accessibility states
    - any failures are written down before the phase is considered ready for execution
  </acceptance_criteria>
  <done>Phase 5 has concrete verification evidence that the artifact surface is safe and usable.</done>
</task>
</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- accessibility and live-region polish in `ArtifactsSurface.svelte`
- fail-closed preview behavior and recovery checks
- phase verification evidence for ARTF-03
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard / AT input → artifact chrome | accessibility controls must remain in host UI and not depend on sandboxed content |
| preview failure → shell state | when preview content or CSP wrapping fails, the UI must show an error state rather than trying to render unsafe fallback content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Denial of Service | runaway artifact preview | mitigate | keep stop/reload outside the sandbox and ensure stop/dismiss is recoverable |
| T-05-06 | Information Disclosure | empty fallback rendering | mitigate | remove the iframe from DOM when preview data is missing or invalid and show an explicit error state |
| T-05-SC | Accessibility regression | artifact chrome | mitigate | verify exact roles, labels, focus rings, and live-region behavior from UI-SPEC |
</threat_model>

<verification>
- svelte-check passes with no errors
- backend sanitizer and preview wrapper tests still pass
- manual keyboard and screen-reader checks validate the UI-SPEC behavior
</verification>

<success_criteria>
- Artifact preview accessibility is explicit and stable under hostile content
- Failure modes are visibly safe and do not silently degrade into unsafe rendering
- Phase 5 has enough evidence to move confidently into execution
</success_criteria>

<output>
Create `.planning/phases/05-artifacts/05-03-SUMMARY.md` when done
</output>
